# =========================================
# SERVER 1: The Main Web App (Port 8080)
# =========================================
server {
    listen 8080;
    server_name localhost;
    client_max_body_size 10M;
    error_page 404 /errors/404.html;

    location / {
        allowed_methods GET;
        root /home/claghrab/Desktop/mini-web/server-tests/main;
        index index.html;
        autoindex on;
    }

    location /uploads/ {
        # client_max_body_size 1B;
        allowed_methods GET POST DELETE;
        root /home/claghrab/Desktop/mini-web/server-tests/main;
        upload_dir /home/claghrab/Desktop/mini-web/server-tests/main/uploads;
    }

    location /cgi-bin/ {
        allowed_methods GET POST;
        root /home/claghrab/Desktop/mini-web/server-tests/main;
        autoindex on;
    }
}

# =========================================
# SERVER 2: The API (Port 8080 - Virtual Host)
# =========================================
server {
    listen 8080;
    server_name api.local;
    client_max_body_size 1M;

    # 1. Broad match
    location /api/ {
        allowed_methods GET POST;
        root /home/claghrab/Desktop/mini-web/server-tests/api;
        autoindex on;
    }

    location / {
        allowed_methods GET;
        root /home/claghrab/Desktop/mini-web/server-tests/main;
        index index.html;
        autoindex on;
    }

    # 2. Longest prefix trap (Overrides /api/ with stricter rules)
    location /api/readonly/ {
        allowed_methods GET;
        root /home/claghrab/Desktop/mini-web/server-tests/api;
        autoindex on;
    }

    # 3. Alias test
    location /assets/ {
        allowed_methods GET;
        alias /home/claghrab/Desktop/mini-web/server-tests/shared_assets/;
        autoindex on;
    }
}

# =========================================
# SERVER 3: The Legacy Server (Port 8081)
# =========================================
server {
    listen 8081;
    server_name legacy.local;

    location / {
        allowed_methods GET;
        root /home/claghrab/Desktop/mini-web/server-tests/legacy;
        index default.html;
    }

    location /docs/ {
        allowed_methods GET;
        root /home/claghrab/Desktop/mini-web/server-tests/legacy;
        autoindex on;
    }

    # HTTP Redirection Test
    location /old-docs/ {
        allowed_methods GET;
        redirect 301 /docs/;
    }
}