server {
    listen localhost:8000;
    location /usr/bin/ {
        allowed_methods GET;
        cgi .sh;
    }
}

server {
    listen localhost:8000;
    location / {
        allowed_methods GET;
        #index passwd;
        autoindex on;
        cgi .sh;
        # index passwd;
    }
    server_name hey;
}